<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="lohitprojects.bitcointrack">

    <!-- Include required permissions for Google Mobile Ads to run. -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".SplashScreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
        <!-- This meta-data tag is required to use Google Play Services. -->
        <meta-data
            android:name="com.google.android.gms.version"
            android:value="@integer/google_play_services_version" />

        <activity
            android:name=".one_bitcoin_data"
            android:label="@string/title_activity_one_bitcoin_data" /> <!-- Include the AdActivity configChanges and theme. -->
        <activity
            android:name="com.google.android.gms.ads.AdActivity"
            android:configChanges="keyboard|keyboardHidden|orientation|screenLayout|uiMode|screenSize|smallestScreenSize"
            android:theme="@android:style/Theme.Translucent" />
        <activity
            android:name=".banner_ad_activity"
            android:label="@string/title_activity_banner_ad_activity" />
        <activity
            android:name=".choose_currency"
            android:label="@string/title_activity_choose_currency" />
        <activity
            android:name=".bitcoin"
            android:label="@string/title_activity_bitcoin" />
        <activity
            android:name=".Mining"
            android:label="@string/title_activity_mining" />
        <activity
            android:name=".Wallets"
            android:label="@string/title_activity_wallets" />
        <activity
            android:name=".Yesterdays_rate"
            android:label="@string/title_activity_yesterdays_rate" />
        <activity
            android:name=".HistoricalData"
            android:label="@string/title_activity_historical_data" />
        <activity
            android:name=".maintohistorical"
            android:label="@string/title_activity_maintohistorical" />
        <activity
            android:name=".latestHash"
            android:label="@string/title_activity_latest_hash" />
        <activity
            android:name=".MiningInfo"
            android:label="@string/title_activity_wallet_info" />
        <activity
            android:name=".NavigationActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar"></activity>
        <activity
            android:name=".aboutUs"
            android:label="@string/title_activity_about_us"
            android:theme="@style/AppTheme.NoActionBar"></activity>
    </application>

</manifest>